{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1><br><br>

    <table class="table">
        <tbody>

            <tr>
                <th>Statut</th>
                <td>{{ inscription.statut }}</td>
            </tr>

            <tr>
                <th>Employe</th>
                <td>{{ inscription.employe.login }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inscription_index') }}">Retourner à la liste</a>

    <a href="{{ path('inscription_edit', {'id': inscription.id}) }}">Modifier</a>

    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}
