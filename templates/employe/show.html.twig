{% extends 'base.html.twig' %}

{% block title %}Employe{% endblock %}

{% block body %}
    <h1 align="center">Employe</h1><br><br>
    <div class="container" align="center">
        <table class="row col-6 shadow-lg p-3 mb-5 bg-body rounded">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ employe.id }}</td>
                </tr>
                <tr>
                    <th>Identifiant</th>
                    <td>{{ employe.login }}</td>
                </tr>
                <tr>
                    <th>Mdp</th>
                    <td>{{ employe.mdp }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ employe.nom }}</td>
                </tr>
                <tr>
                    <th>Prenom</th>
                    <td>{{ employe.prenom }}</td>
                </tr>
                <tr>
                    <th>Statut</th>
                    <td>{{ employe.statut }}</td>
                </tr>
            </tbody>
        </table>
    <a href="{{ path('employe_index') }}">Retourner à la liste</a><br>

    <a href="{{ path('employe_edit', {'id': employe.id}) }}">Modifier</a><br>
    {{ include('employe/_delete_form.html.twig') }}
    </div>
{% endblock %}
