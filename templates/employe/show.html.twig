{% extends 'base.html.twig' %}
{% block stylesheets %} <link rel="stylesheet" href="{{ asset('CSS/style.css') }}">{% endblock %}

{% block title %}Employe{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
<a class="navbar-brand" href="#"><img width="75" src="https://cdn.discordapp.com/attachments/755825390806761655/953271623614607380/logoSymfony.jpg" />    Projet Symfony</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="http://localhost/symfony/formation">  Formations</a>
        <a class="nav-link" href="http://localhost/symfony/inscription"> Liste des inscriptions</a>
        <a class="nav-link" href="http://localhost/symfony/employe"> Employes</a>
      </div>
    </div>
  </div>
</nav><br>
    <h1 align="center">Employe</h1><br><br>
    <div class="container" align="center">
        <table class="row col-6 shadow-lg p-3 mb-5 bg-body rounded">
            <tbody>

                <tr>
                    <th>Identifiant</th>
                    <td>{{ employe.login }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ employe.nom }}</td>
                </tr>
                <tr>
                    <th>Prenom</th>
                    <td>{{ employe.prenom }}</td>
                </tr>
                <tr>
                    <th>Statut</th>
                    <td>{{ employe.statut }}</td>
                </tr>
            </tbody>
        </table>
    <a href="{{ path('employe_index') }}" class="btn btn-secondary">Retourner à la liste</a><br><br>

    <a href="{{ path('employe_edit', {'id': employe.id}) }}" class="btn btn-success">Modifier</a><br><br>
    {{ include('employe/_delete_form.html.twig') }}
    </div>
{% endblock %}
